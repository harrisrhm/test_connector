// For the default version
// import algoliasearch from 'algoliasearch';

// const client = algoliasearch('', '');
// const index = client.initIndex('');

// console.debug(await index.getSettings())

// try {
//     await index.searchForFacetValues('', '').then(({ facetHits }) => {
//         console.log(facetHits);
//     })
// } catch (e) {
//     console.error(e);
// }

// // Search the "category" facet for values matching "phone" in records
// // having "Apple" in their "brand" facet.
// index.searchForFacetValues('framing', 'close').then(({ facetHits }) => {
//     console.log(facetHits);
//   });

